<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rxandroid" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light"
        android:name=".App">

        <activity android:name=".SampleDirectoryActivity" android:label="@string/samples">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FunnyRepresentativesActivity"
            android:label="@string/app_name" />
        <activity android:name=".ImperativeWageCalculatorActivity"
            android:label="Imperative Wage Calculator"/>
        <activity android:name=".ReactiveWageCalculatorActivity"
            android:label="Reactive Wage Calculator"/>
    </application>

</manifest>
